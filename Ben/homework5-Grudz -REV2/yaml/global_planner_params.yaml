allow_unknown: true # Good if not fixed map. If robot hasn't traversed area, it will still create a plan
planner_window_x: 0.0 # Infinite range
planner_window_y: 0.0
default_tolerance: 0.0 # min distance to cool
visualize_potential: false # try setting to true
